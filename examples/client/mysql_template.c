// object_mysql_1.c

#include "liblwm2m.h"
#include "lwm2mclient.h"

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <ctype.h>

// Inlcude the .h generated by the gen_object tool
#include "object_mysql__FILEINDEX_.h"

// Object dimensioning are defined in lwm2mclient.h

/*
 * Multiple instance objects can use userdata to store data that will be shared between the different instances.
 * The lwm2m_object_t object structure - which represent every object of the liblwm2m as seen in the single instance
 * object - contain a chained list called instanceList with the object specific structure mysql__FILEINDEX__instance_t:
 */
typedef struct _mysql__FILEINDEX__instance_
{
    /*
     * The first two are mandatories and represent the pointer to the next instance and the ID of this one. The rest
     * is the instance scope user data (uint8_t test in this case)
     */
    struct    _mysql__FILEINDEX__instance_ * next;    // matches lwm2m_list_t::next
    uint16_t shortID;                                 // matches lwm2m_list_t::id
    // double   resource[ResourceLength__FILEINDEX_];
    int      resource[ResourceLength__FILEINDEX_];   
   
} mysql__FILEINDEX__instance_t;


static void mysql__FILEINDEX__output_buffer(uint8_t * buffer, int length)
{
    int i;
    uint8_t array[16];

    i = 0;
    while (i < length)
    {
        int j;
        fprintf(stderr, "  ");

        memcpy(array, buffer+i, 16);

        for (j = 0 ; j < 16 && i+j < length; j++)
        {
            fprintf(stderr, "%02X ", array[j]);
        }
        while (j < 16)
        {
            fprintf(stderr, "   ");
            j++;
        }
        fprintf(stderr, "  ");
        for (j = 0 ; j < 16 && i+j < length; j++)
        {
            if (isprint(array[j]))
                fprintf(stderr, "%c ", array[j]);
            else
                fprintf(stderr, ". ");
        }
        fprintf(stderr, "\n");

        i += 16;
    }
}

static uint8_t mysql__FILEINDEX__read(uint16_t instanceId,
                        int * numDataP,
                        lwm2m_data_t ** dataArrayP,
                        lwm2m_object_t * objectP)
{
    mysql__FILEINDEX__instance_t * targetP;
    int  i, j, r;
    char c;
    int  numres = 0;

   
    targetP = (mysql__FILEINDEX__instance_t *)lwm2m_list_find(objectP->instanceList, instanceId);
    if (NULL == targetP) return COAP_404_NOT_FOUND;

    // Get the number of actual resources that can be read (not executed)
    numres = 0;
    for (i = 0 ; i < ResourceLength__FILEINDEX_ ; i++) {
        if (ro__FILEINDEX_.type[i] != 'e') {
           numres++;
        }	   
    }
   
    if (*numDataP == 0) {

        *dataArrayP = lwm2m_data_new(numres);       
        if (*dataArrayP == NULL) {
            // printf("mysql__FILEINDEX__read: returns:  COAP_500_INTERNAL_SERVER_ERROR\n");
           return COAP_500_INTERNAL_SERVER_ERROR;
        }
       
        *numDataP = numres;
              
	// Copy all the resources, except the executables
	i = 0;
        for (j = 0 ; j < ResourceLength__FILEINDEX_; j++) {
	    if (ro__FILEINDEX_.type[j] != 'e') {
               // printf("copying: %d  %d  type=%c\n",j,ro__FILEINDEX_.resources[j],ro__FILEINDEX_.type[j]);
               (*dataArrayP)[i].id = ro__FILEINDEX_.resources[j];
	       i++;
	    }
        }
       
    }

    // Go through the number of resources (the readable ones)
    for (i = 0 ; i < *numDataP ; i++) {

        if ((*dataArrayP)[i].type == LWM2M_TYPE_MULTIPLE_RESOURCE) {
           return COAP_404_NOT_FOUND;
	}
        // Recources :  15000/0/5700, 15000/0/5701, 15000/0/5702, 15000/0/5703, ...       
        for (j = 0 ; j < ResourceLength__FILEINDEX_; j++) {
	   
           r = ro__FILEINDEX_.resources[j];
           c = ro__FILEINDEX_.type[j];
       
	   // If the resource (e.g. 5700) matches, then...
           if (  (*dataArrayP)[i].id == r ) {
	      
              if (c == 'f') {
                 lwm2m_data_encode_float(atof(ro__FILEINDEX_.value[j]), *dataArrayP + i);
              }
	      
              if (c == 'i') {
                 lwm2m_data_encode_int(atoi(ro__FILEINDEX_.value[j]), *dataArrayP + i);
              }
       
              if (c == 's') {
                 lwm2m_data_encode_string(ro__FILEINDEX_.value[j], *dataArrayP + i);
              }

              if (c == 't') {
                 lwm2m_data_encode_int(time(NULL), *dataArrayP + i);
              }
	      
              if (c == 'o') {       
                 lwm2m_data_encode_string(ro__FILEINDEX_.value[j], *dataArrayP + i);
              }
	      
              if (c == 'b') {
		 if (strcmp(ro__FILEINDEX_.value[j],"false"))  {
                    lwm2m_data_encode_bool(1, *dataArrayP + i);
		 } else  {
                    lwm2m_data_encode_bool(0, *dataArrayP + i);
	         }
              }
	             
          }
        }
               
    }
   
    return COAP_205_CONTENT;
}

static uint8_t mysql__FILEINDEX__discover(uint16_t instanceId,
                            int * numDataP,
                            lwm2m_data_t ** dataArrayP,
                            lwm2m_object_t * objectP)
{
    int i, j, r;
    bool found;

    // printf("Calling: _discover\n");
   
    // Is the server asking for the full object ?
    if (*numDataP == 0) {
       
       *dataArrayP = lwm2m_data_new(ResourceLength__FILEINDEX_);
       
       if (*dataArrayP == NULL) {
          printf("mysql__FILEINDEX__discover: returns:  COAP_500_INTERNAL_SERVER_ERROR\n");       
          return COAP_500_INTERNAL_SERVER_ERROR;
       }
       
       // The number of resources
       *numDataP =  ResourceLength__FILEINDEX_;
      
       // Recources :  15000/0/5700, 15000/0/5701, 15000/0/5702, 15000/0/5703, ...
       for (j = 0 ; j < ResourceLength__FILEINDEX_ ; j++) {
           (*dataArrayP)[j].id = ro__FILEINDEX_.resources[j];
       }
       
    } else {
       
       for (i = 0; i < *numDataP; i++) {

           // Recources :  15000/0/5700, 15000/0/5701, 15000/0/5702, 15000/0/5703, ...       
           found = false; 
           for (j = 0 ; j < ResourceLength__FILEINDEX_; j++) {
              r = ro__FILEINDEX_.resources[j];
              if (  (*dataArrayP)[i].id == r ) {
                  found = true;
              }
           }
       
           if (found == false) {
              return COAP_404_NOT_FOUND;       
           }
       }
       
    }
 
    if (found == true){	
       return COAP_204_CHANGED;
    }
   
   
    return COAP_205_CONTENT;
}

static uint8_t mysql__FILEINDEX__write(uint16_t instanceId,
                         int numData,
                         lwm2m_data_t * dataArray,
                         lwm2m_object_t * objectP)
{
    mysql__FILEINDEX__instance_t * targetP;
    int i, j, r;
    bool  found;

    targetP = (mysql__FILEINDEX__instance_t *)lwm2m_list_find(objectP->instanceList, instanceId);
    if (NULL == targetP) return COAP_404_NOT_FOUND;

    for (i = 0 ; i < numData ; i++) {

        // Recources :  15000/0/5700, 15000/0/5701, 15000/0/5702, 15000/0/5703, ...       
        found = false; 
      
        for (j = 0 ; j < ResourceLength__FILEINDEX_; j++) {
           r = ro__FILEINDEX_.resources[j];
           if ( dataArray[i].id == r ) {
	      
              // if (1 != lwm2m_data_decode_float(dataArray + i, &(targetP->resource[j]))) {	      
              // if (1 != lwm2m_data_decode_int(dataArray + i, targetP->resource[j])) {
              //   return COAP_400_BAD_REQUEST;
              // }
              found = true;	      
           }
        }
       
        if (found == false) {
           return COAP_404_NOT_FOUND;       
        }

    }

    return COAP_204_CHANGED;
}


uint8_t mysql__FILEINDEX__update(int instance, int resource, char *value) {
   
    lwm2m_object_t * objectP = GLtestobject__FILEINDEX_;
    mysql__FILEINDEX__instance_t * targetP;
    int j, r;
    bool found;

    uint16_t instanceId = instance;
    targetP = (mysql__FILEINDEX__instance_t *)lwm2m_list_find(objectP->instanceList, instanceId);
    if (NULL == targetP) return COAP_404_NOT_FOUND;
   
    found = false; 
     
    for (j = 0 ; j < ResourceLength__FILEINDEX_; j++) {
        r = ro__FILEINDEX_.resources[j];
        if ( resource == r ) {	   
	   
           //targetP->resource[j] = atof(value);
           targetP->resource[j] = atoi(value);	   
	   strcpy(ro__FILEINDEX_.value[j],value);
	   
           found = true;	   
        }
    }
       
    if (found == false) {
       return COAP_404_NOT_FOUND;       
    }
       
    return COAP_204_CHANGED;
}

static uint8_t mysql__FILEINDEX__delete(uint16_t id,
                          lwm2m_object_t * objectP)
{
    mysql__FILEINDEX__instance_t * targetP;

    objectP->instanceList = lwm2m_list_remove(objectP->instanceList, id, (lwm2m_list_t **)&targetP);
    if (NULL == targetP) return COAP_404_NOT_FOUND;

    lwm2m_free(targetP);

    return COAP_202_DELETED;
}

static uint8_t mysql__FILEINDEX__create(uint16_t instanceId,
                          int numData,
                          lwm2m_data_t * dataArray,
                          lwm2m_object_t * objectP)
{
    mysql__FILEINDEX__instance_t * targetP;
    uint8_t result;


    targetP = (mysql__FILEINDEX__instance_t *)lwm2m_malloc(sizeof(mysql__FILEINDEX__instance_t));
    if (NULL == targetP) {
       printf("mysql__FILEINDEX__create: returns:  COAP_500_INTERNAL_SERVER_ERROR\n");       
       return COAP_500_INTERNAL_SERVER_ERROR;
    }
   
    memset(targetP, 0, sizeof(mysql__FILEINDEX__instance_t));

    targetP->shortID = instanceId;
    objectP->instanceList = LWM2M_LIST_ADD(objectP->instanceList, targetP);

    result = mysql__FILEINDEX__write(instanceId, numData, dataArray, objectP);

    if (result != COAP_204_CHANGED)
    {
        (void)mysql__FILEINDEX__delete(instanceId, objectP);
    }
    else
    {
        result = COAP_201_CREATED;
    }

    return result;
}

static uint8_t mysql__FILEINDEX__exec(uint16_t instanceId,
                        uint16_t resourceId,
                        uint8_t * buffer,
                        int length,
                        lwm2m_object_t * objectP)
{

    if (NULL == lwm2m_list_find(objectP->instanceList, instanceId)) return COAP_404_NOT_FOUND;

    switch (resourceId)
    {
    case 1:
        return COAP_405_METHOD_NOT_ALLOWED;
    case 2:
        fprintf(stdout, "\r\n-----------------\r\n"
                        "Execute on %hu/%d/%d\r\n"
                        " Parameter (%d bytes):\r\n",
                        objectP->objID, instanceId, resourceId, length);
        mysql__FILEINDEX__output_buffer((uint8_t*)buffer, length);
        fprintf(stdout, "-----------------\r\n\r\n");
        return COAP_204_CHANGED;
    case 3:
        return COAP_405_METHOD_NOT_ALLOWED;
    default:
        return COAP_404_NOT_FOUND;
    }
}

void mysql__FILEINDEX__display(lwm2m_object_t * object) {
   
    if (  ro__FILEINDEX_.active == 0 ) {
       return;
    }
   
#ifdef WITH_LOGS
    fprintf(stdout, "  /%u: Test object, instances:\r\n", object->objID);
    mysql__FILEINDEX__instance_t * instance = (mysql__FILEINDEX__instance_t *)object->instanceList;
    while (instance != NULL)
    {
        fprintf(stdout, "    /%u/%u: shortId: %u, test: %u\r\n",
                object->objID, instance->shortID,
                instance->shortID, instance->test);
        instance = (mysql__FILEINDEX__instance_t *)instance->next;
    }
#endif
}

lwm2m_object_t * mysql__FILEINDEX__get(void) {
   
    // Initialize objects, and resources
    Resource__FILEINDEX__Object_Config();      
   
    lwm2m_object_t * testObj;

    testObj = (lwm2m_object_t *)lwm2m_malloc(sizeof(lwm2m_object_t));
    int j;
   
    if (NULL != testObj) {
        int i;
        mysql__FILEINDEX__instance_t * targetP;

        memset(testObj, 0, sizeof(lwm2m_object_t));

        testObj->objID = ro__FILEINDEX_.object;
       
        int instanceStart = 0;
       
        // Define Instances:  15000/0, 15000/1, 15000/2, 15000/3, ....
    
        for (i=0 ; i < ro__FILEINDEX_.instances; i++) {
       
            targetP = (mysql__FILEINDEX__instance_t *)lwm2m_malloc(sizeof(mysql__FILEINDEX__instance_t));
            if (NULL == targetP) return NULL;
           
            memset(targetP, 0, sizeof(mysql__FILEINDEX__instance_t));
            
	    targetP->shortID = instanceStart + i;
	    
	    // Assign the resource a value
            for (j = 0 ; j < ResourceLength__FILEINDEX_; j++) {
               targetP->resource[j]  = ro__FILEINDEX_.resources[j];
	    }
	          
            testObj->instanceList = LWM2M_LIST_ADD(testObj->instanceList, targetP);
       
        }
       
        /*
         * From a single instance object, two more functions are available.
         * - The first one (createFunc) create a new instance and filled it with the provided informations. If an ID is
         *   provided a check is done for verifying his disponibility, or a new one is generated.
         * - The other one (deleteFunc) delete an instance by removing it from the instance list (and freeing the memory
         *   allocated to it)
         */
       
        testObj->readFunc     = mysql__FILEINDEX__read;
        testObj->discoverFunc = mysql__FILEINDEX__discover;
        testObj->writeFunc    = mysql__FILEINDEX__write;
        testObj->executeFunc  = mysql__FILEINDEX__exec;
        testObj->createFunc   = mysql__FILEINDEX__create;
        testObj->deleteFunc   = mysql__FILEINDEX__delete;
    }

    return testObj;
}

void mysql__FILEINDEX__free(lwm2m_object_t * object)
{
    LWM2M_LIST_FREE(object->instanceList);
    if (object->userData != NULL)
    {
        lwm2m_free(object->userData);
        object->userData = NULL;
    }
    lwm2m_free(object);
}

